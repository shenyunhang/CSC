TRAIN:
        SCALES: [300]
        # TODO(YH): 为了batchsize32
        MAX_SIZE: 300
        IMS_PER_BATCH: 32
        ROIS_PER_IM: 2048
        SNAPSHOT_ITERS: 1
        USE_PREFETCH: True
        PROPOSAL_METHOD: gt
        ASPECT_GROUPING: False

TEST:
        SCALES: [300]
        MAX_SIZE: 2000
        NMS: 0.5

RNG_SEED: 6
EXP_DIR: ssd
USE_ROI_SCORE: True
